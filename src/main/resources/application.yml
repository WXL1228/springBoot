server:
  port: 9090 #端口号

#  数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://121.41.171.58:3306/springboot?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&useSSL=false&useSSl=false
    username: root
    password: Admin@123456
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true   # ??? SQL?????
        use_sql_comments: true  # ???? SQL ????????????

#Mybatis
mybatis:
  #  映射xml
  mapper-locations: classpath:mapper/*.xml
  configuration:
    #   配置日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true


#logging:
#  file:
#    name: logs/app.log          # ???????????????????
#    # path: /var/logs           # ?????????????? spring.log
#  level:
#    com.example: DEBUG           # ??????????????
#    org.hibernate.SQL: DEBUG   # ?? SQL
#    org.hibernate.orm.jdbc.bind: TRACE  # ?? SQL ???Spring Boot 3.x / Hibernate 6 ?????
#    org.springframework.web: DEBUG
#    l.BasicBinder: TRACE